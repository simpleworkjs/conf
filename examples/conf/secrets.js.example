/**
 * Secrets configuration template
 *
 * IMPORTANT: This is a template file. Copy it to secrets.js and fill in your actual values.
 *
 * DO NOT commit secrets.js to version control!
 * Add this line to your .gitignore:
 *   conf/secrets.js
 *
 * These settings override both base.js and environment-specific configs.
 */

module.exports = {
	database: {
		username: 'your-db-username',
		password: 'your-db-password'
	},

	api: {
		// API keys and tokens
		token: 'your-api-token-here',
		secretKey: 'your-secret-key-here'
	},

	// Third-party service credentials
	services: {
		stripe: {
			secretKey: 'sk_test_...',
			publishableKey: 'pk_test_...'
		},
		sendgrid: {
			apiKey: 'SG...'
		},
		aws: {
			accessKeyId: 'AKIA...',
			secretAccessKey: 'your-secret-access-key'
		}
	},

	// Session secret
	session: {
		secret: 'your-session-secret-here'
	},

	// Encryption keys
	encryption: {
		key: 'your-encryption-key-here',
		iv: 'your-initialization-vector'
	}
};
